{
  "type": "postgres",
  "host": "--SET HOST--",
  "port": "--SET PORT--",
  "username": "--SET USER--",
  "password": "--SET PASSWORD--",
  "database": "--SET DB NAME--",
  "dropSchema": false,
  "synchronize": true,
  "logging": true,
  "entities": [
    "dist/apps/backend/api/src/**/*.entity{.ts, .js}"
  ],
  "migrations": [
    "dist/apps/backend/api/migrations/*{.ts, .js}"
  ],
  "cli": {
    "migrationsDir": "db/migrations"
  }
}
